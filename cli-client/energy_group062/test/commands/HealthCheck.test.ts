import {expect, test} from '@oclif/test'
//import axios from 'axios'
//import moxios from 'moxios'
//import sinon from 'sinon'
//import { equal } from 'assert'

describe('HealthCheck', () => {
/*  
  beforeEach(function () {
    moxios.install()
  })

  afterEach(function () {
    moxios.uninstall()
  })

  test
  .stdout()
  .command(['HealthCheck'])
//  .it('runs health check', ctx => {
//    expect(ctx.stdout).to.contain('{ status: \'OK\' }')

  .it('runs health check', function (done) {
    moxios.stubRequest('https://localhost:8765/energy/api/HealthCheck', {
        status: 200,
        responseText: 'OK'
    })

    moxios.wait(function () {
        equal(onFulfilled.getCall(0).args[0].data, 'OK')
        done()
    })

    
  })*/
})
